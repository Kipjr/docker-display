name: Shellcheck

on: 
  push:
    paths:
      - '**.sh'
#    branches-ignore:
#      - master
  workflow_dispatch:

permissions: {}
jobs:
  shellcheck:
    name: Shellcheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@latest
      - name: Run ShellCheck
        uses: ludeeus/action-shellcheck@master
